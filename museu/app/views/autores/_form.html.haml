= simple_form_for(@autor) do |f|
  = f.error_notification

  .form-inputs
    = f.input :nome
    = f.input :nacionalidade
    = f.input :resumoBiografia, label:"Resumo da Biografia"

  .form-actions
    = f.button :submit, 'Salvar'
